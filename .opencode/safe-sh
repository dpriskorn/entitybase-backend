#!/bin/sh
# Generated by chatgpt 2026-01-23
prev=""
for arg in "$@"; do
  if [ "$prev" = "docker" ]; then
    case "$arg" in
      up|build|run)
        echo "❌ Blocked command: $*, ask user to run the CI"
        exit 1
        ;;
    esac
  fi
  prev="$arg"
done
